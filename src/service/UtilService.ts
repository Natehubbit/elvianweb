import { INITIAL_ORDER_VALUES } from "../common/constants";
import { IOrderForm } from "../types/forms";

export default class UtilService {
  static orderFormValidate(values:IOrderForm){
    // console.log('sss',values)
    // return Object
    //   .keys(values)
    //   .map((k: keyof IOrderForm)=>values[k])
  }
}